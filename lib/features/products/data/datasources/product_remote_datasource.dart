/// Data source remoto para productos
/// Aquí van las llamadas directas a Supabase/API
class ProductRemoteDataSource {
  // Inyectar SupabaseClient aquí
  // ProductRemoteDataSource({required this.supabaseClient});
  
  /// Obtiene los productos de Supabase
  // Future<List<ProductModel>> getProducts() async {
  //   return await supabaseClient
  //     .from('products')
  //     .select()
  //     .then((data) => List<ProductModel>.from(
  //       data.map((x) => ProductModel.fromJson(x))
  //     ));
  // }
}
